{
  "name": "tinytech",
  "version": "1.0.2",
  "description": "for microservices",
  "main": "dist/main.js",
  "scripts": {
    "dev": "npm run build && npm run start",
    "test": "npm run build && jest",
    "build": "rm -rf dist && tsc",
    "start": "node dist/main.js",
    "release": "npm run test && git add . && git commit -m \"release\" && git push origin master"
  },
  "repository": "https://github.com/arkjxu/tinytech.git",
  "author": "Ark Xu",
  "license": "MIT",
  "private": false,
  "dependencies": {},
  "devDependencies": {
    "eslint": "^6.8.0",
    "jest": "^25.5.3",
    "typescript": "^3.8.3"
  }
}
